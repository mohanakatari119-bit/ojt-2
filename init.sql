-- Database is created by Docker with the env vars.
-- Tables are created automatically by SQLAlchemy on startup.
-- This file can be used for seed data.

-- Example: seed a default service (optional, comment out if not needed)
-- INSERT INTO services (id, name, description) VALUES
--   (gen_random_uuid(), 'example-service', 'Auto-seeded example service')
--   ON CONFLICT DO NOTHING;
